<div class="card">
    <div class="card-body">

        <h4>Categorias</h4>
        {{#each categorias}}
        <a href="/categorias/{{slug}}">
            <h5>{{nome}}</h5>
        </a>
        {{else}}
        <5>Sem categorias cadastradas</5>
        {{/each}}
    </div>
</div>